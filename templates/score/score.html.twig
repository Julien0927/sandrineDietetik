{% extends 'base.html.twig' %}

{% block title %}Avis recette{% endblock %}

{% block body %}
<div id="recipeOpinion">
        <div class="fullcard">
            {{ form_start(scoreForm, {'attr': {'class': 'ajax-form'}}) }}
        </div>
        <div class="mt-4">
            <div class="opinionRecipe d-flex justify-content-center">
                <p>Vous avez testé la recette :  </p>
                <h5 >{{ recipe.title }}</h5>
            </div>
            <div class="mb-3 mx-5">
                {{ form_row(scoreForm.opinion) }}
              <div class="mt-3">
                {{ form_row(scoreForm.note) }}
              </div>
            </div>
        </div>
        <div class="d-flex justify-content-center mx-5">
            {{ form_end(scoreForm) }}
        <div id="ajax-messages"></div>
        </div>
</div>
{% endblock %}